# Supabase Credentials Configuration
# Copy this file to credentials.txt and fill with your actual credentials

# ============================================================
# OPTION 1: ANON_KEY Mode (Recommended - Simpler)
# ============================================================
# Use this for basic access without user authentication
# Only needs URL and ANON_KEY (no email/password required)

AUTH_MODE=ANON_KEY
SUPABASE_URL=https://xxxxx.supabase.co
SUPABASE_ANON_KEY=eyJhbGc...your_long_anon_key_here

# ============================================================
# OPTION 2: EMAIL_PASSWORD Mode (Full Authentication)
# ============================================================
# Use this if you need user-specific data with RLS policies
# Requires email/password AND anon key

#AUTH_MODE=EMAIL_PASSWORD
#SUPABASE_URL=https://xxxxx.supabase.co
#SUPABASE_ANON_KEY=eyJhbGc...your_long_anon_key_here
#EMAIL=test@ofxsurfing.com
#PASSWORD=yourpassword

# ============================================================
# NOTES:
# ============================================================
# - ANON_KEY mode: Simpler, no user login, uses anon permissions
# - EMAIL_PASSWORD mode: Full auth, user-specific data, RLS policies
# - For testing, ANON_KEY mode is easier to start with
# - Get URL and ANON_KEY from: Supabase Dashboard → Settings → API
